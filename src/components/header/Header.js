import React from "react";

import { Typography, AppBar, Toolbar } from "@material-ui/core";

const Header = () => {
  return (
    <AppBar>
      <Toolbar>
      <Typography>Udaan App (Beta)</Typography>
      </Toolbar>
    </AppBar>
  );
};

export default Header;
